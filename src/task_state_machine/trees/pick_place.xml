<root BTCPP_format="4">
    <BehaviorTree ID="MainTree">
        <Sequence>
            <DetectObject pose="{pick_pose}"/>
            <CallPickObject pose="{pick_pose}"/>
            <CallPlaceObject pose="{place_pose}"/>
        </Sequence>
    </BehaviorTree>
</root>
